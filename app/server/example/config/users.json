// This JSON is parsed:
// - so a line beginning // denotes a comment;
// - `// ...` ending a line will also be considered a comment, as long as it contains no `"`
{
   // Administrator
   "admin": {
      "id": 1,
      "email" : "admin@myexamplecorp.com",
      "role": "admin",
      "name": "A. Administrator",
      "permissions": {
         // Disable any by-default-enabled admin permissions by specifying them with value 0 here.
         // "createContainerReservation": 0,
         // "startContainer": 0,
         // "stopContainer": 0,
         // "removeContainer": 0,
         // "developContainers": 0,
         // "setContainerViewers": 0,
         // "setContainerDevelopers": 0,
         // "getContainerLogs": 0,
         // "viewAllContainers": 0,
         // "manageUsers": 0  // Deny user/role management even for this admin user
      },
      "resources": {
         
         "profiles": [ "*" ],
         "networks": [ "*" ],
         "runtimes": [ "*" ],
         "IDEs":     [ "*" ],
         "images":   [ "*" ],
         "auth":     [ "*" ] 
      },
      "ssh": {
         // SSH public keys allowed to ssh to containers this user owns or is a developer of
         // Use named entries, e.g. "publicKeys": { "laptop": "ssh-rsa AAAA... user@laptop" }
         //  "publicKeys": { "laptop": "ssh-rsa AAAAB... admin@myexamplecorp.com" }
         "publicKeys": {},

         // SSH keypair(s) added to integrated ssh-agent running in IDE context
         "keypairs": {
            "*": {
               // "private": "-----BEGIN OPENSSH PRIVATE KEY-----\n..."
               "private": "", 
               // "public": "ssh-rsa AAAAB... admin@myexamplecorp.com"
               "public": ""
            }
         }
      },
      // Optional: GitHub Personal Access Token passed as GH_TOKEN to all containers
      // launched by this user, enabling 'gh pr checkout' and other gh CLI operations.
      // Requires 'repo' scope for private repos; 'public_repo' scope for public repos.
      "gh_token": ""
   }
   // Example developer user (add/manage users like this via: dockside user create)
   // "johnsmith": {
   //    "id": 2,
   //    "email": "john.smith@someservice.com",
   //    "role": "developer",
   //    "name": "John Smith",
   //    "permissions": {
   //       // Grant or deny individual permissions beyond what the role provides.
   //       // "createContainerReservation": 1,
   //       // "startContainer": 1,
   //       // "stopContainer": 1,
   //       // "removeContainer": 1,
   //       // "developContainers": 1,
   //       // "setContainerViewers": 1,
   //       // "setContainerDevelopers": 1
   //    },
   //    "resources": {
   //       "profiles": { "admin-only-profile": 0 },
   //       "networks": { "*": 1, "custom-network-we-want-disabled": 0 },
   //       "runtimes": { "sysbox-runc": 0, "gvisor": 1 },
   //       "IDEs":     [ "openvscode/latest" ]
   //    },
   //    "ssh": {
   //       "publicKeys": {}
   //    }
   //    // "gh_token": "ghp_xxxxxxxxxxxxxxxxxxxx"
   // }
}

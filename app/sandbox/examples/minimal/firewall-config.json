{
  "networks": {
    "dockside": {
      "_comment": "Applies to devtainer egress only — the dockside container's own traffic is exempted via dockside_egress (default \"exempt\") in network-config.json (see its comment there).",
      "egress": [
        { "comment": "DNS (UDP and TCP) to any resolver",
          "proto": "udp", "ports": [53], "to": "all" },
        { "proto": "tcp", "ports": [53], "to": "all" },

        { "comment": "Standard web traffic",
          "proto": "tcp", "ports": [80, 443], "to": "all" },

        { "comment": "Default deny — drop everything else",
          "action": "drop" }
      ]
    }
  }
}
